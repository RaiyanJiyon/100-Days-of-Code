# Prompting the user to input their name and storing it in the variable 'user_name'
user_name = input("What's Your Name? ")

# Calculating the length of the input string and storing it in the variable 'length'
length = len(user_name)

# Printing the length of the input string
print(length)
