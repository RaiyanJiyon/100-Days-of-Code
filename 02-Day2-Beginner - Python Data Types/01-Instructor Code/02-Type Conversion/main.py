# Prompting the user to input their name and storing it in the variable 'name_char'
name_char = input("What's Your Name? ")

# Calculating the length of the input string and storing it in the variable 'length'
length = len(name_char)

# Printing the length of the input string along with a message
print("Your name has " + str(length) + " characters")
